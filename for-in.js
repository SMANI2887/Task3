//for-in conditions

//1.For the given JSON iterate over all for loops (for, for in, for of, forEach)

let a = {
    name : "mani",
    no : 28,
};
for(let key in a){
console.log("key :", key);
};